name: Organizar Biblioteca por Tipo

on:
  push:
    paths:
      - 'Biblioteca/biblioteca.json'  # Activar flujo cuando se modifique biblioteca.json

jobs:
  organize-library:
    runs-on: ubuntu-latest

    steps:
      # 1. Descargar el repositorio
      - name: Checkout del repositorio
        uses: actions/checkout@v3

      # 2. Verificar existencia de biblioteca.json y validarlo
      - name: Verificar existencia de biblioteca.json y validarlo
        run: |
          BIBLIOTECA_JSON="Biblioteca/biblioteca.json"
          
          # Verificar si el archivo JSON existe
          if [ ! -f "$BIBLIOTECA_JSON" ]; then
            echo "El archivo $BIBLIOTECA_JSON no existe."
            exit 1
          fi

          # Verificar que el archivo JSON sea válido
          if ! jq empty "$BIBLIOTECA_JSON" > /dev/null; then
            echo "Error: El archivo $BIBLIOTECA_JSON tiene un formato inválido."
            exit 1
          fi
          echo "El archivo biblioteca.json es válido."

      # 3. Leer y procesar biblioteca.json
      - name: Crear carpetas y actualizar README.md
        run: |
          BIBLIOTECA_JSON="Biblioteca/biblioteca.json"
          
          # Leer los tipos únicos de libros
          types=$(jq -r '.books[].type' "$BIBLIOTECA_JSON" | sort | uniq)

          # Procesar cada tipo de libro
          for type in $types; do
            # Crear la carpeta para cada tipo si no existe
            type_dir="Libros/$type"
            mkdir -p "$type_dir"

            # Ruta al README.md dentro de la carpeta
            readme="$type_dir/README.md"

            # Crear o modificar el README.md
            if [ ! -f "$readme" ]; then
              echo "# 📚 Libros de $type" > "$readme"
              echo "Bienvenido a la colección de libros de **$type**. Aquí encontrarás los libros clasificados por este género." >> "$readme"
              echo "---" >> "$readme"
              echo "" >> "$readme"
            fi

            # Añadir libros al README.md
            books=$(jq -c ".books[] | select(.type==\"$type\")" "$BIBLIOTECA_JSON")
            for book in $books; do
              title=$(echo "$book" | jq -r '.title')
              author=$(echo "$book" | jq -r '.author')
              status=$(echo "$book" | jq -r '.status')
              favorite=$(echo "$book" | jq -r '.favorite')

              # Verificar si el libro ya está en el README
              if ! grep -q "## $title" "$readme"; then
                echo "## $title" >> "$readme"
                echo "- **Autor**: $author" >> "$readme"
                echo "- **Estado**: $status" >> "$readme"
                echo "- **Favorito**: $favorite" >> "$readme"
                echo "" >> "$readme"
                echo "### Comentarios" >> "$readme"
                echo "> Añade aquí tus comentarios sobre este libro." >> "$readme"
                echo "---" >> "$readme"
                echo "" >> "$readme"
              fi
            done
          done

      # 4. Hacer commit y push de los cambios
      - name: Commit y push de los cambios
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add Libros/
          git commit -m "Organizar biblioteca por tipo y actualizar README.md de libros" || echo "No hay cambios para commitear."
          git push
